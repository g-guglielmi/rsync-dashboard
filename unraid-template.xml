<?xml version="1.0"?>
<Container version="2">
  <Name>rsync-dashboard</Name>
  <Repository>ghcr.io/g-guglielmi/rsync-dashboard:latest</Repository>
  <Registry/>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support/>
  <Project>https://github.com/g-guglielmi/rsync-dashboard</Project>
  <Overview>Read-only dashboard for your unRAID rsync backup jobs. Reads the log files your existing rsync scripts already write and shows per-job status, duration, data transferred, and files deleted. Never touches the scripts themselves and can't trigger backups.</Overview>
  <Category>Tools: Backup:</Category>
  <WebUI>http://[IP]:[PORT:8686]/</WebUI>
  <TemplateURL/>
  <Icon>https://raw.githubusercontent.com/g-guglielmi/rsync-dashboard/main/icon.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires/>

  <Config Name="WebUI Port" Target="8686" Default="8686" Mode="tcp" Description="Port to reach the dashboard from a browser" Type="Port" Display="always" Required="true" Mask="false">8686</Config>

  <Config Name="Logs Folder" Target="/data/logs" Default="/mnt/user/appdata/rsync_logs" Mode="ro" Description="The folder your rsync scripts already write their logs to (LOG_DIR in your scripts). Mounted read-only -- the dashboard cannot modify or delete anything here." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/rsync_logs</Config>

  <Config Name="Timezone" Target="TZ" Default="Europe/Rome" Mode="" Description="Timezone so log timestamps display correctly" Type="Variable" Display="always" Required="false" Mask="false">Europe/Rome</Config>

  <Config Name="History Limit" Target="HISTORY_LIMIT" Default="15" Mode="" Description="How many past runs to keep per job" Type="Variable" Display="advanced" Required="false" Mask="false">15</Config>

  <Config Name="Cache Seconds" Target="CACHE_SECONDS" Default="20" Mode="" Description="How long results are cached before logs are re-read" Type="Variable" Display="advanced" Required="false" Mask="false">20</Config>

  <!-- Alerts are configured from the dashboard itself (gear icon, top right) and saved in the State Folder. -->
  <Config Name="State Folder" Target="/data/state" Default="/mnt/user/appdata/rsync-dashboard" Mode="rw" Description="Where alert settings (from the gear icon in the dashboard) and already-sent-alert memory are stored. Keep this mapped or your settings vanish on the next container update." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/rsync-dashboard</Config>

  <Config Name="PUID" Target="PUID" Default="99" Mode="" Description="User ID the app runs as. 99 = unRAID's nobody, so the State Folder is writable. Change only if your appdata is owned by another user." Type="Variable" Display="advanced" Required="false" Mask="false">99</Config>
  <Config Name="PGID" Target="PGID" Default="100" Mode="" Description="Group ID the app runs as. 100 = unRAID's users group." Type="Variable" Display="advanced" Required="false" Mask="false">100</Config>

  <Config Name="Settings Password" Target="SETTINGS_PASSWORD" Default="" Mode="" Description="Optional but recommended: the dashboard has no login, so anyone on your network can open it. With a password set, changing alert settings or sending test messages requires it (viewing the dashboard stays open)." Type="Variable" Display="always" Required="false" Mask="true"></Config>
</Container>
